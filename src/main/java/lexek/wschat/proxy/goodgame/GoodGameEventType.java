package lexek.wschat.proxy.goodgame;

public enum GoodGameEventType {
    WELCOME,
    JOIN,
    SUCCESS_JOIN,
    FAILED_JOIN,
    BAD_RIGHTS,
    AUTH,
    SUCCESS_AUTH,
    FAILED_AUTH,
    MESSAGE,
    BAN,
    USER_BAN,
    ERROR
}
